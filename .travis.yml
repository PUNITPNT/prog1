matrix:
 include:
  - language: c
    sudo: false # only for faster builds
    compiler: 
     - clang
     - gcc
    script:
      - gcc -o p2 p2.c // we can use gcc instead of clang too
      - ./p2
